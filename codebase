1)Reduced end-to-end pipeline latency by 40% through SQL execution-plan optimization, size-based micro-batching, and parallel
thread execution across Spring Boot ingestion services.

Analyze this ETL codebase and determine whether the following optimization claims are accurate:

1. SQL execution plan optimization
- Were SQL queries optimized?
- Any index improvements?
- Query rewrites?
- Explain Plan usage?

2. Micro batching
- Are records processed in batches?
- Batch size configuration?
- Evidence of batching improvements?

3. Parallel execution
- ExecutorService?
- CompletableFuture?
- parallelStream?
- Spring @Async?
- Thread pools?

4. Pipeline latency reduction
- Any logs showing before/after timings?
- Any metrics proving performance improvement?

Find actual implementation evidence and point to code files.


------------------------------
Here are the 6 prompts, one per bullet:

---

**Bullet 1 — ETL / Data Pipeline (1M+ records, 99.99% integrity)**
```
Search this codebase for ETL or data pipeline logic. Look for: volume indicators (batch sizes, record counts, loop bounds), data quality checks (schema validation, reconciliation, null checks, SLA assertions). What does the code actually process and validate? Give me exact class/method names, any numeric constants you find, and what checks are enforced. If you cannot confirm 1M+ records or 99.99% integrity from code alone, draft a resume bullet with the accurate numbers and checks you do find.
```

---

**Bullet 2 — 40% Latency Reduction (micro-batching, parallel threads, query optimization)**
```
Search this codebase for performance optimization patterns: micro-batching logic, thread pools, parallel execution, and any SQL query tuning (indexes, query hints, pagination). Find exact class/method names, thread pool configs, batch size values, and any before/after comments or benchmarks. If no 40% figure is measurable from code, draft a resume bullet using what you actually find — include the specific techniques and any config values.
```

---

**Bullet 3 — PCF → OpenShift Migration + GitHub Actions + Harness**
```
Look through this codebase and config files for cloud migration artifacts: any PCF (manifest.yml, cf push) references, OpenShift/OCP configs (Deployment YAMLs, routes, imagestreams), GitHub Actions workflows (.github/workflows/), and Harness pipeline references. List what you find exactly. If you can confirm a migration happened, summarize what changed. If not fully verifiable, draft a resume bullet that accurately describes the deployment setup you can actually see.
```

---

**Bullet 4 — Notification Microservice (35% integration time cut, 100% duplicate logic removed)**
```
Find any notification service in this codebase. Look for: REST endpoints handling notification/alert dispatch, template handling, any async patterns, and evidence this service replaced or consolidated older duplicated logic (dead code, deprecation comments, changelogs). List endpoint names, template types supported, and which teams or consumers are referenced. If the 35% or 100% figures can't be confirmed, draft a resume bullet using what the code actually shows.
```

---

**Bullet 5 — Kafka Tuning (<100ms latency, 3x peak traffic)**
```
Search for Kafka configuration in this codebase: consumer group IDs, partition counts, fetch sizes, polling intervals, offset management, and any load or throughput handling logic. List all Kafka config values you find (application.yml, properties files, annotations). If there are comments or benchmarks referencing latency or traffic spikes, include them. If <100ms or 3x peak can't be confirmed from code, draft a resume bullet using the actual Kafka setup you find.
```

---

**Bullet 6 — Liquibase + Jenkins Automation (80% manual overhead reduction)**
```
Look for Liquibase and Jenkins integration in this codebase: changelog files, shell scripts invoking Liquibase, Jenkinsfile pipelines, and any environment-specific deployment logic (staging vs production). List the scripts, pipeline stages, and what was automated. If there are comments about manual steps that were replaced, include them. If the 80% figure isn't verifiable, draft a resume bullet that accurately reflects the automation you actually find.
```

---

Each prompt is self-contained and tells Copilot exactly what to look for, what to report, and what to do if the number can't be confirmed. Paste the Copilot output back here and I'll help you rewrite the bullets cleanly.



2)
Act as a Staff Backend Engineer and Senior Interviewer.

Analyze this ENTIRE codebase and validate whether the following resume claims are technically accurate.

For EACH claim:

1. Find evidence in code/config/deployment files.
2. Point to exact files/classes/functions.
3. Explain HOW the implementation works.
4. Explain what interview questions could come from it.
5. Tell me if claim is:
   - STRONG (fully defensible)
   - PARTIAL (needs softer wording)
   - REMOVE (overclaiming)

Also explain concepts deeply so I can defend them in interviews.

Claims to validate:

------------------------------------------------

1. ETL PIPELINES

"Designed and owned ETL pipelines processing financial records across portfolio analytics workflows"

Questions:

- Where is ingestion happening?
- Where is transformation happening?
- Where is distribution happening?
- Which components are ETL?
- How many jobs/services exist?
- Is ownership claim justified?

------------------------------------------------

2. PERFORMANCE

"Reduced pipeline latency through SQL optimization, batching improvements, and parallel execution"

Find:

- SQL optimizations
- batching logic
- ExecutorService
- CompletableFuture
- parallelStream
- Spring @Async
- thread pools
- parallel consumers

Questions:

- Did batching actually happen?
- Is parallel execution true?
- Is SQL execution optimization true?
- Can latency reduction be measured?

Provide evidence.

------------------------------------------------

3. KAFKA

Claim:

"Contributed to Kafka event-driven processing"

Find:

- Kafka consumers
- producers
- partition configuration
- consumer groups
- offset handling
- retry handling
- DLQ handling
- lag monitoring

Questions:

- Did I tune Kafka?
- Or only work with Kafka?

Recommend exact resume wording.

------------------------------------------------

4. OPENSHIFT / DEVOPS

Claims:

"OpenShift deployment"
"CI/CD"
"Jenkins"

Find:

- deployment yaml
- OpenShift manifests
- Jenkinsfiles
- Harness pipelines
- Dockerfiles

Explain deployment flow:

Code
→ Build
→ Test
→ Artifact
→ Deploy

Questions:

- What exactly happens in deployment?
- What role did I play?

------------------------------------------------

5. API CONTRACTS

Claim:

"Defined API contracts"

Find:

- REST controllers
- Swagger/OpenAPI
- DTOs
- request validation

Questions:

- Did I actually define APIs?
- Or consume existing APIs?

Recommend wording.

------------------------------------------------

6. SCHEMA VERSIONING

Find:

- schema evolution
- backward compatibility
- versioned DTOs
- migrations

Question:

Did schema versioning actually happen?

------------------------------------------------

7. RCA / PRODUCTION SUPPORT

Claim:

"Led production incident RCA"

Find:

- Splunk usage
- logs
- monitoring
- debugging workflow

Questions:

- How are incidents debugged?
- How is root cause found?

Generate interview answers.

------------------------------------------------

8. DQ / DATA QUALITY

Find:

- DQ jobs
- validation jobs
- schema checks
- reconciliation
- monitoring

Explain:

What DQ means in THIS codebase.

------------------------------------------------

9. DATA MOVEMENT JOBS

Find:

- DM jobs
- orchestration
- movement logic

Explain:

What they do.

------------------------------------------------

10. DEPLOYMENT MIGRATION

Claim:

"EPL → EPLX migration"
"Harness → OCP migration"

Find evidence.

Explain architecture before and after.

------------------------------------------------

OUTPUT FORMAT:

Claim:
Evidence:
Files:
Interview explanation:
Interview questions:
Resume wording recommendation:

Also identify resume overclaims.

Prioritize technical accuracy over making resume impressive.




3)
I want to understand how Kafka is configured and used in this codebase so I can accurately describe it on my resume. Please analyze the code and tell me:

Consumer group configuration — what are the group.id, max.poll.records, fetch.min.bytes, fetch.max.wait.ms, session.timeout.ms values set to, if any?
Partition setup — how many partitions are configured per topic? Is partition assignment manual or automatic?
Consumer scaling — how many consumer instances are deployed? Does the instance count match partition count?
Lag monitoring — is there any lag tracking, alerting, or dashboard integration (Splunk, JMX, Kafka metrics)?
Throughput handling — are there any retry mechanisms, dead letter queues, or backpressure handling?
Peak load handling — are there any comments, configs, or batch-size settings that suggest design decisions around high-volume periods?

Based on what you find, help me write a single resume bullet point that accurately describes my Kafka work — using only what exists in the code, no assumptions.




----------------------------------------------


Search this codebase for ETL or data pipeline logic. Look for: volume indicators (batch sizes, record counts, loop bounds), data quality checks (schema validation, reconciliation, null checks, SLA assertions). What does the code actually process and validate? Give me exact class/method names, any numeric constants you find, and what checks are enforced. If you cannot confirm 1M+ records or 99.99% integrity from code alone, draft a resume bullet with the accurate numbers and checks you do find.
Search this codebase for performance optimization patterns: micro-batching logic, thread pools, parallel execution, and any SQL query tuning (indexes, query hints, pagination). Find exact class/method names, thread pool configs, batch size values, and any before/after comments or benchmarks. If no 40% figure is measurable from code, draft a resume bullet using what you actually find — include the specific techniques and any config values.
Look through this codebase and config files for cloud migration artifacts: any PCF (manifest.yml, cf push) references, OpenShift/OCP configs (Deployment YAMLs, routes, imagestreams), GitHub Actions workflows (.github/workflows/), and Harness pipeline references. List what you find exactly. If you can confirm a migration happened, summarize what changed. If not fully verifiable, draft a resume bullet that accurately describes the deployment setup you can actually see.
Find any notification service in this codebase. Look for: REST endpoints handling notification/alert dispatch, template handling, any async patterns, and evidence this service replaced or consolidated older duplicated logic (dead code, deprecation comments, changelogs). List endpoint names, template types supported, and which teams or consumers are referenced. If the 35% or 100% figures can't be confirmed, draft a resume bullet using what the code actually shows.
Search for Kafka configuration in this codebase: consumer group IDs, partition counts, fetch sizes, polling intervals, offset management, and any load or throughput handling logic. List all Kafka config values you find (application.yml, properties files, annotations). If there are comments or benchmarks referencing latency or traffic spikes, include them. If <100ms or 3x peak can't be confirmed from code, draft a resume bullet using the actual Kafka setup you find.
Look for Liquibase and Jenkins integration in this codebase: changelog files, shell scripts invoking Liquibase, Jenkinsfile pipelines, and any environment-specific deployment logic (staging vs production). List the scripts, pipeline stages, and what was automated. If there are comments about manual steps that were replaced, include them. If the 80% figure isn't verifiable, draft a resume bullet that accurately reflects the automation you actually find.
